--- a/virtinst/domain/clock.py
+++ b/virtinst/domain/clock.py
@@ -71,9 +71,8 @@
         pit.name = "pit"
         pit.tickpolicy = "delay"
 
-        hpet = self.timers.add_new()
-        hpet.name = "hpet"
-        hpet.present = False
+        # Batocera's QEMU build lacks the legacy -no-hpet CLI switch that
+        # libvirt emits for hpet.present=no, so keep the default machine HPET.
 
         if guest.hyperv_supported() and self.conn.support.conn_hyperv_clock():
             hyperv = self.timers.add_new()
