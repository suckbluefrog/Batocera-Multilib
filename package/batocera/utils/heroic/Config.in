config BR2_PACKAGE_HEROIC
	bool "heroic (AppImage)"
	depends on BR2_x86_64
	help
	  Heroic Games Launcher packaged as an AppImage runtime wrapper.

comment "heroic needs an x86_64 target"
	depends on !BR2_x86_64

